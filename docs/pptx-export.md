# PPTX Export

The exporter maps supported schema elements to editable PowerPoint objects where possible. Text, basic shapes, and tables are mapped natively in the current implementation. Images, icons, and charts need asset resolution and richer OOXML mapping before production completion.
