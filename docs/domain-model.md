# Domain Model

The main hierarchy is User -> Project -> Presentation. Presentations own conversations, briefings, storyline versions, slides, assets, generation jobs, imports, exports, and usage records.

Every user has a private workspace. The MVP uses USER and ADMIN roles only.
