# Testing

The repository uses Vitest for package tests. Planned test layers include integration tests for Postgres, Redis, MinIO, SMTP, provider contracts, worker orchestration, visual regression tests, and PPTX structural validation.
