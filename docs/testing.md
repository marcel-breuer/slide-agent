# Testing

The repository uses Vitest for package tests. Planned test layers include integration tests for Postgres, Redis, local file storage, SMTP, provider contracts, worker orchestration, visual regression tests, and PPTX structural validation.
