# PPTX Import

The importer validates that files are ZIP-based PowerPoint packages and checks required OOXML parts before processing. Unsupported objects must be reported instead of silently removed.

Current implementation performs safe package inspection. Detailed editable conversion remains a known limitation.
