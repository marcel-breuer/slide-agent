import { SimpleRoutePage } from "@/components/simple-route-page";

export default function ProjectsPage() {
  return <SimpleRoutePage protectedRoute title="Projects" description="Private project list scoped to the current user." />;
}
