import { SimpleRoutePage } from "@/components/simple-route-page";

export default function SecuritySettingsPage() {
  return <SimpleRoutePage protectedRoute title="Security settings" description="Password changes, sensitive sessions, and security audit events." />;
}
