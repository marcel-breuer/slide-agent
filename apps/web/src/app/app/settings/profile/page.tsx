import { SimpleRoutePage } from "@/components/simple-route-page";

export default function ProfileSettingsPage() {
  return <SimpleRoutePage protectedRoute title="Profile settings" description="Display name, time zone, currency, data export, and account deletion." />;
}
