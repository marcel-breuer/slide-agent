import { SimpleRoutePage } from "@/components/simple-route-page";

export default function ProviderSettingsPage() {
  return <SimpleRoutePage protectedRoute title="AI providers" description="BYOK provider setup with encrypted credentials and masked identifiers." />;
}
