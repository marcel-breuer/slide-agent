import { AppShell } from "@/components/app-shell";
import { ReusableAssetWorkspace } from "@/components/reusable-asset-workspace";

export default function TemplatesPage() {
  return (
    <AppShell>
      <ReusableAssetWorkspace />
    </AppShell>
  );
}
