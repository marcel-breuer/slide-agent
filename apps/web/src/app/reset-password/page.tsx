import { SimpleRoutePage } from "@/components/simple-route-page";

export default function ResetPasswordPage() {
  return (
    <SimpleRoutePage
      title="Reset password"
      description="Complete a password reset and invalidate sensitive sessions."
    />
  );
}
