import { SimpleRoutePage } from "@/components/simple-route-page";

export default function LoginPage() {
  return <SimpleRoutePage title="Login" description="Secure email and password login with server-side sessions." />;
}
