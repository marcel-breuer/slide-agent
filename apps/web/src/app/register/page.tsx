import { SimpleRoutePage } from "@/components/simple-route-page";

export default function RegisterPage() {
  return <SimpleRoutePage title="Register" description="Create an account and start the double opt-in verification flow." />;
}
