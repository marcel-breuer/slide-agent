import { SimpleRoutePage } from "@/components/simple-route-page";

export default function ForgotPasswordPage() {
  return (
    <SimpleRoutePage
      title="Forgot password"
      description="Request a single-use expiring password reset token."
    />
  );
}
