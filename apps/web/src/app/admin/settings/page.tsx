import { SimpleRoutePage } from "@/components/simple-route-page";

export default function AdminSettingsPage() {
  return <SimpleRoutePage protectedRoute title="Admin settings" description="Global slide maximum, upload limits, maintenance messages, and policy settings." />;
}
