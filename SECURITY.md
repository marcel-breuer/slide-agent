# Security

Report vulnerabilities privately to the repository owner.

Slide Agent stores AI provider credentials using authenticated encryption and never returns plaintext credentials after submission. Prompt content logging is disabled by default. Private uploads and generated assets must be served through authorization checks and signed URLs.
